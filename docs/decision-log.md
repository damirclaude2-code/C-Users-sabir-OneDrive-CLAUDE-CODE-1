# Decision log

Этот файл нужен, чтобы сохранять долговечные решения и не спорить о них заново без причины.

## Как добавлять решение

### YYYY-MM-DD — короткое название решения

- Контекст:
- Решение:
- Почему так:
- Альтернативы:
- Когда пересмотреть:

## Записи

### 2026-08-27 — Главная рабочая папка живёт на Рабочем столе (OneDrive), не в Downloads

- Контекст: нужно было единое "главное окно" для всей работы с Claude Code.
- Решение: перенести `CLAUDE-CODE-1` из `Downloads`/`Projects` на реальный
  OneDrive-путь Рабочего стола.
- Почему так: пользователю удобнее находить папку там, где он её ожидает
  видеть визуально — на Рабочем столе.
- Альтернативы: оставить в `Downloads\Projects`.
- Когда пересмотреть: если пользователь сменит компьютер или отключит
  синхронизацию OneDrive.

### 2026-08-30 — Ежедневный архив результатов в отдельной папке по датам

- Контекст: пользователь хотел иметь резервную копию всех финальных
  результатов работы "на всякий случай", с возможностью искать по дню.
- Решение: `Cloud Code работа\ДД.ММ.ГГГГ\` — при первой задаче за день
  создаётся подпапка с датой, туда автоматически копируется каждый
  финальный файл (без даты в имени — дата уже в папке).
- Почему так: сначала пробовали дату в имени файла в одной плоской папке —
  оказалось хуже читается и сложнее ориентироваться, чем подпапки по дням.
- Альтернативы: дата в имени файла; отдельная папка на каждую задачу без
  привязки к дате.
- Когда пересмотреть: если архив станет слишком большим для ручного
  просмотра — рассмотреть автоматическую архивацию старых папок.

### 2026-08-30 — PPTX и PDF версии презентации собираются независимыми генераторами

- Контекст: одна и та же презентация должна существовать в двух форматах;
  риск, что баг верстки в одном формате перейдёт в другой при конвертации.
- Решение: `pptxgenjs` для PPTX и `pdfkit` для PDF — два полностью отдельных
  кодовых пути с одной и той же дизайн-системой, но независимой генерацией.
- Почему так: явное требование пользователя — "чтобы не плодить ошибки при
  создании презентации".
- Альтернативы: собрать один формат и экспортировать/конвертировать во
  второй через LibreOffice.
- Когда пересмотреть: если поддержка двух независимых генераторов станет
  слишком дорогой при дальнейших правках дизайна.

### 2026-08-30 — credentials.md сохраняет собственную структуру при новых установках

- Контекст: несколько установочных промптов (search-and-read-web и другие)
  предлагали свой шаблон `SERVICE_API_KEY=` для credentials-файла.
- Решение: не переписывать `credentials.md` под шаблон каждого нового
  промпта — сохранять существующую структуру
  (`### Сервис / Тип / Значение / Где используется / Дата / Комментарий`)
  и просто добавлять в неё новые записи.
- Почему так: явное правило самих установочных промптов — "не ломай
  существующую структуру".
- Альтернативы: переписывать под последний шаблон при каждой установке.
- Когда пересмотреть: не планируется — это принцип файла.

### 2026-08-30 — fact-checking skill осознанно не установлен

- Контекст: source-of-truth файл `06-fact-checking-skill-4-4.md` не был
  передан пользователем вместе с остальными файлами skill pack.
- Решение: не выдумывать содержимое навыка по аналогии с другими — честно
  пометить как отсутствующий в `CLAUDE.md` и в `skill-router`.
- Почему так: правило "не добавляй выдуманные факты" применимо и к
  содержимому самих skill-файлов, не только к обычным ответам.
- Альтернативы: написать `fact-checking` самостоятельно по образцу похожих
  навыков (`copywriter`, `designer`).
- Когда пересмотреть: как только пользователь пришлёт исходный файл.
- Статус: закрыто 2026-09-19 — см. запись ниже.

### 2026-09-19 — fact-checking skill установлен

- Контекст: пользователь прислал недостающий `06-fact-checking-skill-4-4.md`
  (вместе с дубликатами файлов `design-elements-pack`), тем самым выполнив
  условие пересмотра записи от 2026-08-30.
- Решение: создать `.claude/skills/fact-checking/{SKILL.md,README.md}`
  дословно из исходного файла; убрать пометки "не установлен" в `CLAUDE.md`,
  `skill-router/{SKILL.md,README.md,trigger-dictionary.md}`. Заодно добавлен
  недостающий `design-elements-pack/templates/landing-page-skeleton.md` —
  остальные присланные файлы оказались побайтово идентичны уже установленным
  и не тронуты.
- Почему так: source-of-truth файл наконец получен — условие ревизии из
  записи 2026-08-30 выполнено; правило "не выдумывать содержимое skill" не
  нарушалось, т.к. содержимое взято из присланного файла без изменений.
- Альтернативы: нет — единственный путь установить навык корректно.
- Когда пересмотреть: не планируется.

### 2026-09-19 — design-elements-pack обновлён до v3.0.0 из присланных файлов

- Контекст: пользователь прислал `SKILL.md` (v3.0.0) и `README.md` пакета;
  установленные были v1.0.0 — с дополнительным списком файлов и деревом
  структуры, написанными локально. Также прислан новый шаблон
  `sales-slide-skeleton.md`.
- Решение: `SKILL.md` и `README.md` заменены присланными версиями дословно
  (source-of-truth новее); `templates/sales-slide-skeleton.md` добавлен;
  `visual-rules`, `style-override-policy`, `sources-and-licenses`,
  `pricing-page-skeleton` совпали с установленными.
- Почему так: присланные файлы — источник истины и явно новее по версии.
- Альтернативы: сохранить локальную v1.0.0 или слить обе версии — отклонено
  как лишнее усложнение; старые версии остаются в истории git.
- Когда пересмотреть: если понадобится вернуть дерево структуры пакета в
  README — добавить его отдельным блоком, не меняя остальной текст.
### 2026-08-30 — design-elements-pack восстановлен из плоских файлов по имени

- Контекст: установщик ожидал папку `design-elements-pack/` с подпапками
  `references/` и `templates/`, но пользователь прислал 10 файлов россыпью,
  без структуры.
- Решение: восстановить структуру по смыслу имён — 3 файла со словом
  "skeleton" в названии → `templates/`, остальные 7 (правила, каталоги,
  политики) → `references/`.
- Почему так: сама инструкция явно разрешала так поступить в случае, если
  структура папки не видна, вместо того чтобы просить пользователя
  пересобрать архив.
- Альтернативы: попросить пользователя прислать файлы заново в виде
  вложенной папки с явной структурой.
- Когда пересмотреть: если разбивка на references/templates когда-либо
  окажется нелогичной для новых файлов — пересмотреть правило разбивки.

### 2026-08-30 — Завели систему документации `docs/` вместо одного большого CLAUDE.md

- Контекст: `CLAUDE.md` разрастался с каждой установкой skill'ов (297+
  строк) и грозил стать свалкой из фактов, правил и разовых заметок.
- Решение: разделить по назначению — `CLAUDE.md` остаётся коротким
  навигатором и местом для правил поведения; долговечные факты,
  повторяющиеся баги и решения переехали в `docs/project-memory.md`,
  `docs/bugs-and-gotchas.md`, `docs/decision-log.md` соответственно.
- Почему так: один факт — одно каноническое место; иначе при следующих
  правках легко забыть обновить все копии сразу (уже случалось со
  словарём автовыбора навыков).
- Альтернативы: продолжать копить всё в `CLAUDE.md`.
- Когда пересмотреть: если появится задача, для которой три категории
  (факты/баги/решения) окажутся недостаточными — добавить новую категорию
  документа, а не смешивать с существующими.

### 2026-08-31 — Подключён приватный GitHub-репозиторий

- Контекст: пользователь захотел резервную копию в облаке и потренировать
  полный цикл commit → push → PR → merge.
- Решение: подключён приватный репозиторий
  `damirclaude2-code/C-Users-sabir-OneDrive-CLAUDE-CODE-1`, ветка `master`
  переименована в `main` под стандарт GitHub.
- Почему так: приватность — чтобы бизнес-контекст из `docs/project-memory.md`
  не был виден посторонним; `credentials.md` в push не попадает благодаря
  `.gitignore`, но остальная документация — не для чужих глаз.
- Альтернативы: публичный репозиторий; вообще не подключать GitHub.
- Когда пересмотреть: если понадобится делиться репозиторием с другими
  людьми — тогда обсудить права доступа отдельно.

### 2026-08-31 — Notion подключаем через прямой API, не через MCP-коннектор

- Контекст: в каталоге Discover нашёлся официальный (verified) MCP-коннектор
  Notion — рассматривали, подключать ли его вместо прямого API.
- Решение: MCP-коннектор Notion не подключать. Интеграцию делать позже
  напрямую через Notion API (создать integration на notion.so, токен — в
  `credentials.md`, как Tavily/Firecrawl).
- Почему так: осознанный выбор пользователя — предпочёл прямой контроль
  через API. Технической проблемы с самим MCP-коннектором не нашли (он
  официальный, скорее всего не "слабее" API), но решение уже принято не по
  техническим причинам.
- Альтернативы: подключить официальный MCP-коннектор Notion (был доступен,
  от него отказались).
- Когда пересмотреть: если прямая работа с Notion API окажется избыточно
  сложной для задач — вернуться к варианту MCP-коннектора.

### 2026-09-12 — Новые направления работы разделены по подпапкам с локальным CLAUDE.md

- Контекст: пользователь начал вести два новых направления — продажа товаров
  VESSARI (часы, сумки, ремни) на Авито и отдельный бизнес грузоперевозок
  (свой грузовой автомобиль, поиск грузов на биржах). Нужно было решить, как
  организовать файлы, чтобы контекст одного направления не смешивался с
  другим и с основной работой над самим проектом CLAUDE-CODE-1.
- Решение: `Projects/VESSARI/Авито-продажи/` (новый канал сбыта того же
  товара VESSARI) и `Projects/ГРУЗОПЕРЕВОЗКИ/` (отдельный бизнес) — каждая со
  своим локальным `CLAUDE.md`. Локальные файлы действуют только внутри своей
  папки, поверх общих правил корневого `CLAUDE.md` (skill pack, agentic
  triage, proof loop, безопасность — остаются едиными для всего проекта).
- Почему так: Claude Code поддерживает вложенные `CLAUDE.md` — это даёт
  разделение контекста без дублирования общих правил и без необходимости
  заводить отдельные репозитории/папки верхнего уровня.
- Альтернативы: полностью отдельные папки/репозитории вне `CLAUDE-CODE-1` —
  отклонено, так как тогда пришлось бы заново переносить skill pack,
  agentic-triage, proof loop и правила безопасности в каждую.
- Когда пересмотреть: если грузоперевозки или Авито-продажи вырастут
  настолько, что понадобится полностью независимый git-репозиторий (например,
  для отдельной команды с доступом только к своему направлению).

### 2026-09-13 — Skill'ы `yuan`/`numerology-analysis` остаются в этой папке, но изолированы от бизнеса

- Контекст: в проект добавили skill `yuan` (комплексная китайская нумерология/
  гадание) и выделенный из него `numerology-analysis`. Пользователь явно
  попросил, чтобы эта папка не была "привязана" к грузоперевозкам и Авито —
  бизнес не должен пересекаться с личным непрофильным инструментом.
- Решение: физически оставить оба skill'а в `.claude/skills/` внутри
  `CLAUDE-CODE-1` (не выносить на пользовательский уровень
  `~/.claude/skills/`), но зафиксировать изоляцию явно:
  1. Проверено и подтверждено: ни один файл `Projects/ГРУЗОПЕРЕВОЗКИ/`,
     `Projects/VESSARI/`, `docs/` не упоминает `yuan`/нумерологию, и
     наоборот — ни один файл `yuan`/`numerology-analysis` не упоминает
     грузоперевозки/Авито/VESSARI.
  2. Эти два skill'а помечены как личный/непрофильный инструмент — не часть
     официального skill pack (уроки 4.2–4.4) и не участвуют в бизнес-словаре
     автовыбора навыков из `CLAUDE.md`.
  3. При сохранении точки (`git commit`) изменения в `.claude/skills/yuan/` и
     `.claude/skills/numerology-analysis/` коммитятся отдельным коммитом от
     бизнес-изменений (`Projects/ГРУЗОПЕРЕВОЗКИ/…`, `Projects/VESSARI/…`),
     чтобы история git не смешивала личное и бизнес.
- Почему так: пользователь выбрал вариант "оставить в этой же папке, но
  отдельно" вместо переноса на уровень пользователя — было явно предложено
  оба варианта, перенос отклонён.
- Альтернативы: перенос `.claude/skills/yuan` и `numerology-analysis` в
  `C:\Users\sabir\.claude\skills\` (полностью вне репозитория и вне бизнес-
  папки, доступно во всех проектах Claude Code на этом компьютере) —
  отклонено пользователем.
- Когда пересмотреть: если пользователь захочет использовать этот skill в
  других своих проектах Claude Code — тогда перенос на пользовательский
  уровень станет уместным.

### 2026-09-12 — Удалены неиспользуемые папки-заготовки (01_Входящие, 02_Результаты, Knowledge, Operations)

- Контекст: эти 4 папки были частью первоначального плана структуры
  (с 27.08.2026), но за две с лишним недели работы ни разу не использовались.
- Решение: удалить все четыре. Перед удалением проверено: все папки пустые
  (включая скрытые файлы), git их не отслеживал, единственное упоминание
  было в описании структуры в `CLAUDE.md` (тоже обновлено).
- Почему так: их роль на практике закрыли `Projects/` (проекты и направления)
  и `docs/` (долговечная память/заметки). Пользователь явно просил чистое
  рабочее пространство без лишних папок.
- Альтернативы: оставить как задел на будущее — отклонено, пустые папки
  только засоряли контекст, не давая пользы.
- Когда пересмотреть: если понадобится отдельный "инбокс" для входящих
  файлов или отдельное место для регулярных операционных процессов —
  завести заново по конкретной необходимости, а не заранее.

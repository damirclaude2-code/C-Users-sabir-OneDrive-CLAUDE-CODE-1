# CLAUDE.md — правила работы в этой папке

## 1. Кто я и чем занимаюсь
- Зовут: Damir
- Роль: директор
- Компания / проект: VESSARI
- Чем занимается компания: продажа товаров
- Компьютер: Windows
- Уровень технической подготовки: новичок
- Задачи, которые хочу делегировать Claude Code: подготовка документов, анализ папок,
  простые сайты, автоматизации, работа с таблицами, CEO-анализ маркетплейсов,
  создание помощников и ассистентов

Открытый вопрос (без ответа факт не додумываю, см. раздел "Вопросы" внизу):
основные рабочие папки — см. ниже.

## 2. Как со мной работать
- Эта папка — главное рабочее место Damir'а. Все задачи (документы, таблицы, анализ,
  автоматизации, ассистенты) ведутся здесь совместно, через диалог с Claude Code.
- Сначала изучаю контекст: структуру папки, названия файлов, существующие документы.
- Перед изменениями коротко объясняю план: что хочу сделать, зачем, какие файлы/папки затронуты.
- Пишу простым языком, без лишнего технического жаргона.
- Если есть несколько вариантов — даю рекомендуемый вариант, одну реальную альтернативу,
  риск и точный вопрос, на который нужен ответ.

## 3. Режимы работы
- Обсуждение и анализ ("что думаешь?", "посмотри", "что не так?") — НЕ разрешение что-то менять.
  Сначала только анализирую и объясняю.
- Явная просьба ("сделай", "измени", "создай", "перенеси", "исправь") — можно действовать,
  но сначала показываю короткий план.
- Диагностика, тестовые правки, временные файлы, перезапуски, команды с неизвестным
  эффектом — отдельное объяснение и подтверждение.

## 4. Безопасность файлов
- Не удаляю, не перемещаю, не переименовываю и не переписываю файлы без явного подтверждения.
- Не делаю массовые изменения по общей просьбе, пока не покажу список затронутых файлов.
- Не трогаю несвязанные файлы и папки.
- Не откатываю чужие изменения без отдельного разрешения.
- Перед перемещением/удалением сначала предлагаю способ сохранить текущее состояние
  или сделать список для возврата.

## 5. Credentials и приватные данные
- Если в чат передан API-ключ, пароль, токен, webhook URL, логин, приватная ссылка —
  отношусь к этому как к чувствительной информации.
- Не ругаю за передачу ключа в чат — помогаю аккуратно перенести его в `credentials.md`
  и объясняю, как потом ссылаться на него по названию сервиса.
- Не цитирую секреты обратно полностью — только маска, например `sk-...abcd`.
- Не сохраняю секреты в CLAUDE.md, README, публичных документах или заметках.
- Все реальные доступы — только в `credentials.md`, и только по явной просьбе сохранить.
- `credentials.md` защищён через `.gitignore`.
- Если для задачи нужен секрет — сначала проверяю `credentials.md`; если его нет,
  прошу передать доступ или вставить placeholder.

## 6. Структура credentials.md
См. отдельный файл [credentials.md](credentials.md) в этой же папке.

## 7. Организация файлов
Главная папка находится на Рабочем столе: `C:\Users\sabir\OneDrive\Рабочий стол\CLAUDE-CODE-1`
(у Damir'а Рабочий стол синхронизирован через OneDrive, обычный `C:\Users\sabir\Desktop` — не используется).

**Постоянное правило — архив всех результатов работы по дням (с 2026-08-30):**
Архивная папка: `C:\Users\sabir\OneDrive\Рабочий стол\Cloud Code работа`
(название дано пользователем, возможно опечатка «Cloud» вместо «Claude» — уточнить при случае).

- В начале каждого нового календарного дня (по факту первой задачи в этот день) внутри
  архивной папки создаётся подпапка с датой в формате `ДД.ММ.ГГГГ` (например `30.08.2026`),
  если её ещё нет.
- Каждый раз, когда в рамках любой задачи в этот день создаётся финальный файл (отчёт, таблица,
  документ, дашборд и т.п.), его копия автоматически, без отдельного запроса пользователя,
  кладётся в сегодняшнюю подпапку — под обычным именем файла, без даты в названии
  (дата и так видна по папке).
- Это архивная копия "на всякий случай" — оригинал результата остаётся и в рабочей папке
  задачи (если она есть). Так пользователю легко искать всю проделанную работу по дням.

Внутри CLAUDE-CODE-1:
- **01_Входящие** — сюда Damir кидает файлы, которые нужно обработать.
- **02_Результаты** — готовые файлы после работы, которые можно забрать.
- **Projects** — задачи и проекты, у которых есть результат.
- **Knowledge** — заметки, инструкции, идеи, материалы для поиска позже.
- **Operations** — регулярные процессы, настройки, шаблоны, то, что поддерживает работу.

Не создаю случайные файлы в корне папки, если можно выбрать понятное место.
Если структура папок неочевидна — сначала предлагаю простую структуру и жду подтверждения.

## 8. Команды и проверки
- Перед запуском команды объясняю, зачем она нужна и какой результат ожидается.
- Не запускаю разрушительные команды без явного подтверждения.
- Для долгих/неизвестных команд — предлагаю безопасный вариант с ограничением по времени
  или сначала команду только для чтения.
- После действия проверяю результат: перечитываю файл, показываю список изменённых файлов,
  запускаю доступную проверку или объясняю, почему проверка невозможна.

## 9. Завершение задачи
- Не говорю "готово", пока не проверил результат.
- В финале кратко объясняю: что изменилось, где это лежит, как это проверить,
  что осталось непроверенным (если проверить нельзя).
- Если две попытки подряд не дают результата — останавливаюсь, объясняю что уже
  проверено, предлагаю следующий безопасный шаг.

---

Открытых вопросов нет — все ключевые факты о контексте собраны выше.

---

## Auto Prompt Upgrade

Если пользователь пишет "автоулучши запрос", "улучши промпт", "auto update prompt" или дает слишком короткую задачу, сначала улучшай постановку задачи, а уже потом предлагай выполнение.

---

## Search And Read Web

Если задача зависит от актуальной информации, конкретных сайтов, цен, правил,
новостей, конкурентов или возможностей сервисов, сначала ищи и читай
источники, а не отвечай только из памяти.

Если подключены Tavily и Firecrawl:
- используй Tavily для поиска;
- используй Firecrawl для чтения страниц;
- отделяй проверенные факты от предположений;
- сохраняй источники в результате.

---

## Skill Pack

В проекте установлен локальный skill pack — 6 навыков из уроков 4.2-4.4:

- `auto-prompt-upgrade` (урок 4.2) — усиление сырой постановки задачи
- `search-and-read-web` (урок 4.3) — поиск и чтение сайтов (Tavily + Firecrawl)
- `copywriter` (урок 4.4) — прикладные тексты
- `designer` (урок 4.4) — улучшение существующей визуальной подачи
- `design-elements-pack` (урок 4.4) — сборка структуры из готовых блоков + локальная библиотека `references/`/`templates/`
- `skill-router` (урок 4.4) — автовыбор минимального уместного маршрута

⚠ **`fact-checking` НЕ установлен.** Source-of-truth файл
`06-fact-checking-skill-4-4.md` не был передан при установке — без него
нельзя было создать навык, не рискуя выдумать его содержимое. Как только
файл появится, довести установку: создать `.claude/skills/fact-checking/`
по образцу остальных навыков и убрать эту пометку.

⚠ **Автовыбор навыков работает всегда, не только по команде `use skills`.**
Перед любым нетривиальным запросом молча сверяй формулировку со словарём
ниже и применяй подходящий навык сразу — не нужно спрашивать разрешения на
"включение навыка" (это не то же самое, что подтверждение самого действия;
обычные правила про план/подтверждение перед рискованными шагами остаются в
силе). Полная версия словаря и разбор конфликтов между навыками — в
`.claude/skills/skill-router/trigger-dictionary.md`.

### Словарь автовыбора (кратко)

| Если в запросе есть... | Подключай |
|---|---|
| напиши / перепиши / текст / лендинг / оффер / заголовок / CTA / письмо / пост / УТП | `copywriter` |
| улучши дизайн / переделай слайд / нечитаемо / визуально шумно / плохая иерархия / поправь вёрстку | `designer` |
| собери лендинг с нуля / нужна pricing-страница / сравнение тарифов / собери слайд с нуля / какие блоки нужны | `design-elements-pack` |
| найди / поищи / проверь в интернете / актуальные цены / что нового / сайт конкурента | `search-and-read-web` |
| запрос короткий, без цели/формата/файлов, или сразу про крупное необратимое действие | `auto-prompt-upgrade` |
| "не используй design-elements-pack" / "другой визуальный стиль" / "используй мой референс" | override — baseline не применять |

Если совпадений с двумя навыками — собери короткую цепочку (пример: сначала
`design-elements-pack`, потом `copywriter`, если нужна и структура, и текст).
Если совпадений нет и задача при этом нетривиальная — используй обычную
логику без навыка, ничего не выдумывая про "нужный skill".

Помни различие:

- `designer` = улучшение существующего визуального решения;
- `design-elements-pack` = сборка структуры из готовых сильных блоков.

Design baseline:

- по умолчанию для дизайн-задач используй `design-elements-pack`;
- для web-задач можно опираться на бесплатный baseline `shadcn/ui`, `daisyUI`
  и `HyperUI`, а также бесплатный `Tailwind CSS`, если проекту нужен CSS
  utility layer;
- не устанавливай npm-библиотеки автоматически без проверки стека;
- не используй Tailwind Plus или другие платные UI kits;
- если пользователь явно просит другой стиль, не навязывай bundled baseline.

Этот проект (`CLAUDE-CODE-1`) сам не является веб-проектом (нет
`package.json`/`src`/Vite/Tailwind config) — поэтому `shadcn/ui` и `daisyUI`
сюда не устанавливались. Если внутри появится отдельный веб-подпроект,
установку web-библиотек нужно делать точечно в нём, а не в корне.

## Skill Quality Standard

Если в проекте создается или обновляется локальный skill, не оставляй его в
формате простой роли.

Проверяй, что в skill есть:

- понятный `trigger`;
- `sequence canon`;
- `anti-patterns`;
- понятный gate `что считается хорошим результатом`.

Для более сильного уровня по возможности также добавляй:

- `что этот навык чинит`;
- предпочтительный формат ответа;
- distinction rules с похожими skills;
- короткий self-check перед финалом.

Если skill этого не содержит, сначала дотяни его до этой структуры, а уже потом
считай задачу по skill-файлу завершенной.

## Design Baseline

В проекте установлен `design-elements-pack`.

По умолчанию для задач про дизайн, веб-страницы, экраны, слайды, схемы и
визуальную упаковку используй этот pack как базовый visual baseline.

Для веб-задач по умолчанию можно опираться на бесплатный style baseline:

- `Tailwind CSS` как бесплатную open-source CSS базу;
- `shadcn/ui` как источник аккуратных UI primitives и states;
- `daisyUI` как источник theme-aware utility baseline;
- `HyperUI` как источник marketing block patterns;
- локальные `design-elements-pack/references` и `design-elements-pack/templates`.

Важно:

- для совместимого веб-проекта без своей дизайн-системы установи web design
  libraries по правилу style-risk (низкий риск → ставь; высокий/неясный →
  сначала uplift plan и подтверждение);
- если нужна только структура или презентация, используй pack без установки web
  libraries;
- если проект совместим с Tailwind/React/Next/Astro/Vite и пользователь хочет
  code-level реализацию, сначала проверь стек;
- если в проекте уже есть своя дизайн-система, сначала сделай uplift plan и
  спроси подтверждение;
- локальная design library из `design-elements-pack/references` и
  `design-elements-pack/templates` должна быть установлена всегда;
- если пользователь явно просит другой стиль или говорит не использовать
  загруженный baseline, уважай этот override.

Запрещено использовать платные библиотеки как часть установки:

- Tailwind Plus;
- Tailwind UI paid templates;
- Flowbite Pro;
- платные Figma/UI kits.

Override-фразы:

- "не используй design-elements-pack";
- "не используй загруженный дизайн-стиль";
- "не опирайся на shadcn/daisyUI";
- "подбери другой визуальный стиль";
- "используй мой референс вместо библиотеки".
